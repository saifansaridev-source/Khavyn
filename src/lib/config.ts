/**
 * KHAVYN — Global Business Constants
 * Change these values here and they propagate to all checkout, order, and display logic.
 */

/** Flat shipping charge applied to every order (INR). */
export const SHIPPING_FLAT_RATE = 70;
